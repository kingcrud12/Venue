# Flux de données selon l’expérience utilisateur

Format des données :

{

nom : type

}

avec pour type : string, number (int ou double), date (YY-MM-DD), date time (YY-MM-DD H:M:S), date time with UTC, duration (H:M:S:ms), picture (png), enum ( with lists), boolean (true or false), email, link

L’utilisateur Bakary Koné

# L’utilisateur crée un compte puis une publication

A → L’installation

1. Bakary ⇒ Ouverture de l’application
2. Venue ⇒ Loading Page 
3. Venue ⇒ Login or Sign up
4. Sign up
    
    {
    
    {  both or one of them
    
    email : email
    
    telephone : number
    
    }
    
    username : rich string
    
    name : string
    
    password: string
    
    birthday: date
    
    }
    

if Bakary give the phone number go to 

Venue sends an OTP connection code and Bakary goes to Personalization

1. Venue ⇒  Send it to Venue Server ⇒ Identity Provider
2. Identity Provider send UUID to Venue Server ⇒ Venue App
3. Venue Server ⇒ send a confirmation mail to Bakary
4. Bakary ⇒ validate his account on not more than 72 hours
5. Bakary ⇒ connect on Venue App
    1. Failed to password ⇒ Venue sends a connection code OTP
6. Personnalization on option

Preference

{

link: link

refresh_date: datetime

title: string

picture : png (assets + local data)

language: string

}

Personal data

{

Telephone : string

Address : string (separated by comma)

profile pic : png

}

1. Venue ⇒ asks for the location (optional)
2. Venue ⇒ asks for the camera (optional)
3. Venue ⇒ asks for the list of contacts (optional)
4. Venue ⇒  show the tutorial
5. Venue ⇒  show the feed
6. Bakary ⇒ create a  publication
    
    local draft
    
    {
    
    local_id: number ( generated by the app)
    
    time: datetime in UTC
    
    subject: string ⇒ (Venue generated related hashtag)
    
    pictures : list of 
    
    {
    
    picture : png ⇒ (Camera auth or Picture App)
    
    tip : set of { rich string, relative anchor(int, int)}
    
    }
    
    main_text: rich string
    
    }
    
7. Bakary ⇒ post the publication on one of his sites
    1. Draft distant
    2. Private
    3. Friends
    4. Public
    
    {
    
    pub : data of the local draft
    
    date of pub: datetime
    
    site: enum (Draft distant, Private, Friends, Public)
    
    }
    

B→ La déconnexion

# L’utilisateur follow un autre